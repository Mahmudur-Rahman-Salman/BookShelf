import React from "react";

const Navbar = () => {
  return <header className="max-w-screen-2xl mx-auto px-4 py-6">
    <nav>
        {/* left side */}
        <div>Logo</div>
        {/* right side */}
        <div>Nav items</div>
    </nav>
  </header>;
};

export default Navbar;
